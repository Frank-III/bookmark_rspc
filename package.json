{
  "name": "test_bookmarks",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "web:build": "cd client && bun build",
    "web:dev": "cd client && bun dev",
    "generate": "cargo prisma generate",
    "format:check": "prettier -c .",
    "tauri": "tauri"
  },
  "dependencies": {},
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.2",
    "prettier": "^3.0.3",
    "prettier-plugin-rust": "^0.1.9",
    "prettier-plugin-tailwindcss": "^0.1.13"
  }
}
