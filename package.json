{
  "name": "test_bookmarks",
  "version": "0.0.0",
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.2",
    "prettier": "^3.0.3",
    "prettier-plugin-rust": "^0.1.9",
    "prettier-plugin-tailwindcss": "^0.1.13"
  },
  "private": true,
  "scripts": {
    "web:build": "cd client && pnpm build",
    "web:dev": "cd client && pnpm dev",
    "generate": "cargo prisma generate",
    "format:check": "prettier -c .",
    "tauri": "tauri"
  },
  "type": "module",
  "dependencies": {
    "@tanstack/router": "beta"
  }
}
